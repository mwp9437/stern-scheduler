# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Ingest script secrets (service role key — never commit).
scripts/ingest/.env
# Ingest build artifact — regenerated by fall_2026.py --sql-out.
scripts/ingest/_*.sql
# Ingest input snapshots (saved HTML / xlsx). Re-fetched per term, not source.
scripts/ingest/data/
